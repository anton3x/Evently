info:
  name: get /categories
  type: http
  seq: 3
  tags:
    - Categories

http:
  method: GET
  url: "{{baseUrl}}/categories"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/categories"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
