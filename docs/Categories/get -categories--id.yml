info:
  name: get /categories/:id
  type: http
  seq: 4
  tags:
    - Categories

http:
  method: GET
  url: "{{baseUrl}}/categories/:id"
  params:
    - name: id
      value: bdc0e182-f109-4ca9-96ee-3bc4a39e8c3b
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/categories/:id"
      method: GET
      params:
        - name: id
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
