info:
  name: put /categories/:id/archive
  type: http
  seq: 1
  tags:
    - Categories

http:
  method: PUT
  url: "{{baseUrl}}/categories/:id/archive"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/categories/:id/archive"
      method: PUT
      params:
        - name: id
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
