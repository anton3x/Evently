info:
  name: post /categories
  type: http
  seq: 2
  tags:
    - Categories

http:
  method: POST
  url: "{{baseUrl}}/categories"
  body:
    type: json
    data: |-
      {
        "price": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/categories"
      method: POST
      body:
        type: json
        data: |-
          {
            "price": ""
          }
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
