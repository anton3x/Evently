info:
  name: put /categories/:id
  type: http
  seq: 5
  tags:
    - Categories

http:
  method: PUT
  url: "{{baseUrl}}/categories/:id"
  params:
    - name: id
      value: ""
      type: path
  body:
    type: json
    data: |-
      {
        "price": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/categories/:id"
      method: PUT
      params:
        - name: id
          value: ""
          type: path
      body:
        type: json
        data: |-
          {
            "price": ""
          }
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
