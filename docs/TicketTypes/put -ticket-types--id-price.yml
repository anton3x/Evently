info:
  name: put /ticket-types/:id/price
  type: http
  seq: 1
  tags:
    - TicketTypes

http:
  method: PUT
  url: "{{baseUrl}}/ticket-types/:id/price"
  params:
    - name: id
      value: ""
      type: path
  body:
    type: json
    data: |-
      {
        "price": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/ticket-types/:id/price"
      method: PUT
      params:
        - name: id
          value: ""
          type: path
      body:
        type: json
        data: |-
          {
            "price": ""
          }
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
