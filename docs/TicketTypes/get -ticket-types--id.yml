info:
  name: get /ticket-types/:id
  type: http
  seq: 4
  tags:
    - TicketTypes

http:
  method: GET
  url: "{{baseUrl}}/ticket-types/:id"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/ticket-types/:id"
      method: GET
      params:
        - name: id
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
