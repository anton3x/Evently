info:
  name: post /ticket-types
  type: http
  seq: 2
  tags:
    - TicketTypes

http:
  method: POST
  url: "{{baseUrl}}/ticket-types"
  body:
    type: json
    data: |-
      {
        "price": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/ticket-types"
      method: POST
      body:
        type: json
        data: |-
          {
            "price": ""
          }
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
