info:
  name: get /ticket-types
  type: http
  seq: 3
  tags:
    - TicketTypes

http:
  method: GET
  url: "{{baseUrl}}/ticket-types"
  params:
    - name: eventId
      value: ""
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/ticket-types"
      method: GET
      params:
        - name: eventId
          value: ""
          type: query
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
