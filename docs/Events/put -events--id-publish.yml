info:
  name: put /events/:id/publish
  type: http
  seq: 5
  tags:
    - Events

http:
  method: PUT
  url: "{{baseUrl}}/events/:id/publish"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/events/:id/publish"
      method: PUT
      params:
        - name: id
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
