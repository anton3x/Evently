info:
  name: delete /events/:id/cancel
  type: http
  seq: 1
  tags:
    - Events

http:
  method: DELETE
  url: "{{baseUrl}}/events/:id/cancel"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/events/:id/cancel"
      method: DELETE
      params:
        - name: id
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
