info:
  name: get /events
  type: http
  seq: 3
  tags:
    - Events

http:
  method: GET
  url: "{{baseUrl}}/events"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/events"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
