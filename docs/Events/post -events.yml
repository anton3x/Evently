info:
  name: post /events
  type: http
  seq: 2
  tags:
    - Events

http:
  method: POST
  url: "{{baseUrl}}/events"
  body:
    type: json
    data: |-
      {
        "categoryId": "bdc0e182-f109-4ca9-96ee-3bc4a39e8c3a",
        "title": "tititle",
        "price": "0.10",
        "description": "descript",
        "location": "localizacione",
        "startsAtUtc": "2026-06-10T19:00:00Z"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/events"
      method: POST
      body:
        type: json
        data: |-
          {
            "price": ""
          }
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
