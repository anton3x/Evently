info:
  name: put /events/:id/reschedule
  type: http
  seq: 6
  tags:
    - Events

http:
  method: PUT
  url: "{{baseUrl}}/events/:id/reschedule"
  params:
    - name: id
      value: ""
      type: path
  body:
    type: json
    data: |-
      {
        "price": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/events/:id/reschedule"
      method: PUT
      params:
        - name: id
          value: ""
          type: path
      body:
        type: json
        data: |-
          {
            "price": ""
          }
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
